#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"enter the no: ";
    cin>>n;
    if(n%5==0 && n%3==0)cout<<"It is divisible by both";
    else cout<<"It is not divisible by both";
    }